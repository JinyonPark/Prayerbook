-- Local history cutover RPC checks. Documentation for disposable databases.
-- complete_prayer: progress +1, operations for complete remain 0, operation_items +0,
-- idempotent client_event_id does not increment again, no items JSON.
-- reset_current_round: husband selection keeps wife count; wife selection keeps husband count.

select 'documentation-only; do not execute writes against production' as note;
